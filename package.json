{
  "name": "synceditor",
  "version": "0.0.1",
  "description": "Редактор для синхронизаций текстов переводов",
  "main": "index.html",
  "scripts": {
    "deploy": "git checkout gh-pages && git rebase master && git push origin gh-pages && git checkout master && echo \"gh-pages updated!\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Elnur Kurtaliev",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1"
  }
}
